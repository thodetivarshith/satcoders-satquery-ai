name: Backend Lead Akshaya - Final API
on:
  push:
    branches: [main, akshaya/backend-api]
  pull_request:
    branches: [main]
jobes:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: set up python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade
pip
          pip install fastapi uvicorn 
python-multipart pillow
      - name: check backend syntax
        run: |
          python -m py_compile backend/
routes/analyze.py
          echo "Backend syntax OK -Week 2
& 3 DONE"